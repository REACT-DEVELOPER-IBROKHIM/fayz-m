{%- render 'stylesheet', name: 'difference-dna' -%}

<section id='difference-dna' class="difference__wrapper">
  <div class="container">
    <div class="difference__content">
      {% if section.settings.banner_title %}
        <h2>{{ section.settings.banner_title }}</h2>
      {% endif %}
      {% if section.settings.banner_text %}
        <p>{{ section.settings.banner_text }}</p>
      {% endif %}
    </div>
    <div class="difference__box">
      {% if section.settings.banner_video != blank %}
        <video
        class="difference-banner__bg-video"
        crossorigin="anonymous"
        autoplay
        playsinline
        muted
        loop
        preload="none"
        type="video/mp4"
        src="{{ section.settings.banner_video.url }}"></video>
      {% endif %}
      {% if section.settings.banner_image %}
        {{ section.settings.banner_image | image_url: width: 4000  | image_tag }}
      {% endif %}
      </div>
      </div>
</section>

{% schema %}
{
  "name": "difference-dna",
  "settings": [
    {
      "type": "text",
      "id": "banner_title",
      "label": "Difference banner title",
      "default": "Difference banner title"
    },
    {
      "type": "text",
      "id": "banner_text",
      "label": "Difference banner text",
      "default": "Difference banner text"
    },
    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "Banner image"
    },
    {
      "type": "video",
      "id" : "banner_video",
      "label" : "Banner video"
    }
  ],
  "presets": [
    {
        "name": "difference-dna"
    }
  ]
}
{% endschema %}